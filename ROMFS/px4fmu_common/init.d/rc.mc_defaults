#!nsh

set VEHICLE_TYPE mc

if [ $AUTOCNF == yes ]
then
	param set NAV_ACC_RAD 10.0
	param set RTL_RETURN_ALT 60.0
	param set RTL_DESCEND_ALT 10.0
	param set PWM_MIN 1150
	param set PWM_MAX 1750
	param set PWM_RATE 400

	param set RTL_LAND_DELAY 0
fi

# This is the gimbal pass mixer
#set MIXER_AUX octo_x
#set PWM_AUX_RATE 400
#set PWM_AUX_OUT 1234
